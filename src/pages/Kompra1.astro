---
import Layout from "../layouts/Layout.astro";
import Paragraph from "../components/content/Paragraph.astro"

import { getCollection } from 'astro:content';
import { Image } from "astro:assets";

const paragraphs = await getCollection('kompra1');
---

<Layout>
    <section class="p-4 flex flex-col gap-4">
        <h1 class="text-3xl">Standortbestimmung 1 und Zielvereinbarung</h1>

        <Image
            src={"/images/kompra1.png"}
            width={510}
            height={420}
            alt={"Kompra 1"}
        ></Image>
        {
            paragraphs.sort((a,b) => a.data.id - b.data.id).map((paragraph) => (
                <Paragraph content={paragraph} />
            ))
        }

        <h2 class="text-xl">Anhang</h2>
        <a href="/Anhänge/Anhang 1_Umsetzung_OneNote_Teams.pdf" target="_blank" class="hover:underline hover:cursor-pointer">
            Umsetzung OneNote und Teams
        </a>

    </section>
</Layout>